{{ form_start(form, { attr: attr|default({}) }) }}
    {{ form_widget(form) }}

    <input type="submit" value="{{ button_label|default('créer une conversation'|trans) }}"
           class="{{ button_css|default("btn btn-primary") }}" />

    {% if include_back_to_home_link|default(false) %}
        <a href="{{ path('admin_conversation_index') }}" class="btn btn-link">
            {{ 'Retour au liste des conversations'|trans }}
        </a>
    {% endif %}
{{ form_end(form) }}
