{% extends 'admin/layout.html.twig' %}

{% block body %}
    <h1>{{ conversation.response }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{ 'Text'|trans }}</th>
                <td>{{ conversation.input|raw }}</td>
            </tr>
            <tr>
                <th>{{ 'Réponse'|trans }}</th>
                <td>{{ conversation.response|raw }}</td>
            </tr>
        </tbody>
    </table>

    <div class="section">
        <a href="{{ path('admin_conversation_edit', { id: conversation.id }) }}" class="btn btn-lg btn-block btn-success">
            <i class="fa fa-edit"></i> {{ 'Modifier'|trans }}
        </a>
    </div>

    <div class="section">
        {{ include('admin/conversation/_form.html.twig', {
            form: delete_form,
            button_label: 'Supprimer'|trans,
            button_css: 'btn btn-lg btn-block btn-danger',
            show_confirmation: true,
        }, with_context = false) }}
    </div>

{% endblock %}

